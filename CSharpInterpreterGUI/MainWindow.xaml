<Window x:Class="CSharpInterpreterGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="F# Interpreter" Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Height="250" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        
        <TextBox Name="inputTextBox" Height="30" Width="250" Margin="10" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left"
                 ToolTip="Enter expression here"/>

        
        <Button Content="Evaluate" Width="100" Height="30" Grid.Row="0" Grid.Column="1" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center"
                Click="Evaluate_Click"/>

        
        <Label Content="Result:" FontWeight="Bold" Grid.Row="1" Grid.Column="0" Margin="10" VerticalAlignment="Center"/>
        <TextBlock Name="resultTextBlock" FontSize="14" Margin="10,0,0,10" TextWrapping="Wrap" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                   VerticalAlignment="Top" Background="#F0F0F0" Padding="10" Height="Auto" />

    </Grid>
</Window>
