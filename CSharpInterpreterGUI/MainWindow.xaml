<Window x:Class="CSharpInterpreterGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="F# Interpreter" Height="350" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Input -->
        <TextBox Name="inputTextBox" Height="30" Width="300" Margin="10" Grid.Row="0" Grid.Column="0"
                 VerticalAlignment="Center" HorizontalAlignment="Left" ToolTip="Enter expression here" />

        <!-- Buttons -->
        <Button Content="Evaluate" Width="100" Height="30" Grid.Row="0" Grid.Column="1" Margin="10,0,0,0"
                HorizontalAlignment="Left" VerticalAlignment="Center" Click="Evaluate_Click" />
        <Button Content="Help" Width="100" Height="30" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left"
                Margin="10" VerticalAlignment="Center" Click="Help_Click" />

        <!-- New Buttons for Interpolation -->
        <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" Orientation="Vertical" Margin="10">
            <Button Content="Plot Graph" Width="150" Height="30" Margin="0,5,0,0"
                    Click="PlotGraph_Click" />
            <Button Content="Plot Spline Interpolation" Width="150" Height="30" Margin="0,5,0,0"
                    Click="PlotSpline_Click" />
            <Button Content="Plot Linear Interpolation" Width="150" Height="30" Margin="0,5,0,0"
                    Click="PlotLinear_Click" />
        </StackPanel>

        <!-- Result -->
        <Label Content="Result:" FontWeight="Bold" Grid.Row="2" Grid.Column="0" Margin="10" VerticalAlignment="Center" />
        <TextBlock Name="resultTextBlock" FontSize="14" Margin="10,0,0,10" TextWrapping="Wrap" Grid.Row="3" Grid.Column="0"
                   Grid.ColumnSpan="2" VerticalAlignment="Top" Background="#F0F0F0" Padding="10" Height="Auto" />
    </Grid>
</Window>
